/**
 * Power
 * @param {number} m base
 * @param {number} n index
 * @returns {number}
 */


let x = prompt('Ввести число');
let n = prompt('Ввести степень');
function pow(m, n) {
  let x = result;
  result = pow(m, n);
  alert(result);

}
